<%= form_for(@blind_time) do |f| %>
  <% if @blind_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blind_time.errors.count, "error") %> prohibited this blind_time from being saved:</h2>

      <ul>
      <% @blind_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time %><br />
    <%= f.time_select :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
